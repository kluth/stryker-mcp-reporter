name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Quality Gates
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x, 24.x, 26.x]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Dependencies (Tabula Rasa)
        run: |
          rm -f package-lock.json
          npm cache clean --force
          npm install --no-audit --no-fund
        env:
          NODE_OPTIONS: "--max_old_space_size=4096"

      - name: Compile TypeScript
        run: npm run build

      - name: Run Unit Tests (Vitest)
        run: npm run test

      - name: Run Mutation Testing (Stryker)
        run: npm run test:mutation

  release:
    name: Semantic Release
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0 # Erforderlich für die Commit-Analyse von Semantic Release

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "26.x"

      - name: Install Dependencies
        run: npm install --no-audit --no-fund
        env:
          NODE_OPTIONS: "--max_old_space_size=4096"

      - name: Build Project
        run: npm run build

      - name: Run Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release

  announce-release:
    name: Generate & Post Release Announcements
    needs: release
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      discussions: write
      issues: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "26.x"

      - name: Install Dependencies
        run: npm install --no-audit --no-fund

      - name: Generate Multi-Platform Release Announcements
        run: node scripts/generate_release_announcements.js

      - name: Upload Announcement Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-announcements
          path: dist/announcements/

      - name: Post to Discord Webhook (if configured)
        if: env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -H "Content-Type: application/json" -X POST -d @dist/announcements/discord_webhook.json "$DISCORD_WEBHOOK_URL"

